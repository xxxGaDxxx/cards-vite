import type { Meta } from '@storybook/react'

import { CheckEmail } from './checkEmail'

export default {
  title: 'Authorizing/Check Email',
  component: CheckEmail,
  tags: ['autodocs'],
} as Meta<typeof CheckEmail>

export const Default = {}
